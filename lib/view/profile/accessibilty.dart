import 'package:flutter/material.dart';

class Accesibility extends StatelessWidget {
  const Accesibility({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(

    );
  }
}
